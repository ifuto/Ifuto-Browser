# CHROME_SCOPE.md — クローム（chrome/UI）層スコープ裁定台帳

日付: 2026-07-28（§0.1 追記 2026-07-29）/ 対象ブランチ: `arena/019fa7cf-ifuto-browser` / 状態: 裁定ドラフト（コストは全て**推定**、天井は全て**提案**）

## 0.1 最終形の宣言（2026-07-29 ユーザ決定）

**このアプリの製品形は TUI ではない。** 「exe を開いて Edge/Chrome と同じように使う」GUI ブラウザ。
本書の TUI 系天井は「GUI 化までの検証ハーネス（この headless コンテナの唯一の対話面）」として
引き続き拘束するが、TUI 固有表現（セル描画）は描画抽象の一 backend に過ぎない。
- メモリ方針: **理論極限・妥協禁止**。実測で示せない軽さ主張は書かない。
  具体化: DOM をポインタの森から u32 index リンクの巨大配列へ（ユーザ提示「ツリーを Array に」。
  ARCHITECTURE v0.4b として採択。移行前にノード当たりバイト数の現状計測を公開する）。
- JS エンジン: **Akl（自作、C11、JIT なし）** を v0.4 として採択（旧計画の QuickJS 埋め込みは却下）。
- ストリーミング: 「最適化してから処理する」一段構えの禁止。解析・レイアウト・描画は到着データで逐次進める
  （現行 token 駆動 parser は既にこの形）。

## 0.2 v0.2 GUI マイルストーン実績（2026-07-31 記録）

§0.1 の最終形宣言に対する第一段を実装・検証した（詳細な計測は BENCH.md 冒頭節）。
- `build/ifuto-gui`（195,088 B、ldd = vdso/libc/ld のみ）: 生 X11 プロトコル直接実装
  （Xlib/XCB 不使用。CreateWindow/PutImage/GetKeyboardMapping 等、Xauthority 対応）。
- 自前 5x7 ビットマップフォント + 8 セル行ストリップのストリーミング描画（全面バックバッファ非保持）。
- タブ帯/オムニボックス/ステータスバー + キー駆動。IfChrome モデルは TUI と同一（二重実装なし）。
- grid は viewport 窓のみ（`if_render_grid_rows_into`）: 1 万行文書で grid 保有は 38.4 KB。
- chrome ロード経路で **slim-DOM 既定 ON**（script/template 配下は DOM に構築しない）。
- ヘッドレス検証: `--shot OUT.ppm PAGE`（`make guismoke`、32 checks）＋ PNG 目視 QA。
- セッション永続化（達成 2026-08-07）: restore-first 起動（引数指定時も旧タブ温存で
  追加タブ化。autosave の autosave-clobber = データ喪失を構造的に防止）、active 即ロード+
  他 lazy_load、IFUTO_NO_STORE kill switch、omnibox のタブ切替/復元時同期、
  Ctrl+R/r リロード（wingrid 陳腐化付き）、Ctrl+D ブックマーク、3 秒 2 連打 quit 確認。
- 台帳（未達、優先度順）: ~~CJK フォント（現 tofu 表示）~~ → **達成（2026-08-06, 61f8acf）**:
  自前 16x16 全角フォント 215 字（ひらがな・カタカナ全量＋CJK 句読点・全角互換形・
  幾何学図形・矢印・UI 記号、tools/gen_font16.py の 8x8 手設計 → 2x 拡大＋濁点合成。
  外部フォント持ち込みゼロ）。漢字は当面 F16_TOFU の明示印（外枠＋斜線）で区別可能、
  常用漢字の段階投入が続課題 → **第 1 陣達成（2026-08-07）**: 漢字は 8x8 正本では
  画数が潰れるため 16x16 ネイティブ設計（主画 2px = かなと同ウェイト、配置 1px
  分解能）+ ストローク宣言 DSL（H/V/h1/v1/rect/sweep）を gen_font16.py に導入。
  24 字（一二三上下中大小人入山川口日目白百千万円本木年月）、全字を実ラスタで
  目視検査（docs/gui-16px-font-kanji.png）。バイナリ +840B（フォントデータ）。
  未収録は引き続き F16_TOFU 明示。第 2 陣 19 字（表 示 画 文 字 学 生 名 先 今 会 社
  合 同 内 外 左 右 見）も投入（2026-08-07、証跡 docs/gui-16px-font-kanji2.png）。
  第 2 陣で F16_EXTRA_SLOT の u8 桁落ち（glyph 256/257 slot で 0/1 巻き戻り、
  表/見が別字形化する潜在欠陥）を検出・uint16 化で修理。F16_INDEX 側は
  0xFF sentinel 上限の assert を生成器へ追加。回帰は tests/test_font16.c の
  構造検査（昇順/slot 圏内/往復整合/未収録 NULL/両回帰点）+748 checks で固定。
  第 3 陣 20 字（定 実 全 化 行 測 用 検 数 不 計 値 構 分 時 証 書 変 列 存）も投入
  （2026-08-07、証跡 docs/gui-16px-font-kanji3.png）。選定は BENCH/ARCH/CHROME
  台帳語彙の出現頻度上位（測定・実測・検査・証明・構造・系列系）。全字を 8 倍
  ズームの実ラスタで目視検査し、初稿で黒潰れした 数/不/書/存/定/実 を修正。
  疑義切り分けの教訓: 表示スケールで豆腐と密画漢字が見紛うため、字形検査の
  正本は F16_GLYPHS 実データの ASCII dump（f16art 方式）とする。
  続課題: 頻度上位の追加陣（再 換 済 成 能 回 … ）。
  ~~リンクのマウスクリック~~ → **達成（2026-08-07）**:
  fused-fit 成功の単行 ifc 内 <a> のみ表示矩形（IfLSpan）を収集（「メモリは使わなければ
  使わないほど良い」に拠る限定 — 線形 CLI 経路は no_boxlink で矩形収集自体を skip、
  差分は collect の 2 store のみで誤差級）、左クリックで
  ヒットテスト → 相対 join/絶対パスで遷移。http(s)/anchor は未取得のためステータス表示。
  残課題: ホバー。
  ~~複数行 wrap へ逃げたリンク・flatten 経路の矩形未収集~~ → **解消（2026-08-07）**:
  piece 区間 → 大域 seg 添字 → 行ログ交差の 3 段解決で行ごとの部分矩形を収集
  （GUI の全表示リンクがクリック/フォーカス可能）。併発修理: 並列 shard tree
  モードで shard B の span y 未シフト欠陥（serial/parallel span 一致テストで固定）。
  ~~キーボードのリンク操作~~ → **達成（2026-08-07）**: Tab/Shift+Tab（XK_ISO_LEFTTAB
  両受理）でフォーカス巡回（span 無しリンクも href 提示で巡回）、Enter で遷移、
  フォーカスは行 hash salt + paint フックで矩形強調（差分 repaint 整合）。
  クリックもフォーカスを設定（Chrome 流）。副次修正: Ctrl+Tab の比較が '\x09' で
  デッド条件（keysym は常に 0xFF09）だった不具合を修正。ヘッドレス oracle:
  IF_SHOT_FOCUS による focus shot が非差分1帯限定であることを gui_smoke で固定。
  残未達: HTTP 取得（v0.2 後半〜）。セッション復元の GUI 配線・~~italic 傾斜~~ は達成。
  italic は描画後 shear（oblique）: 行毎固定シフト（上ほど +3px、下端 4 行不動で
  アンダーライン直行）、右クリップ・左 bg 補填、全角は 2 セル一括。エンジンは
  st→IF_F_ITALIC→cell は従来どおり流れていたため fb/gui 層の接続のみ（2026-08-07）。
  数値検証: <u><em> の下線行インクランがセル幅ぴったり（80/40/32px）の直行、
  <em> 非下線行に長ランなし。証跡 docs/gui-italic.png。

## 0. 位置づけ

現状の ifuto は**エンジンのみ**でクロームを持たない（`HTML → DOM → CSS → layout → セルグリッド → ANSI/plain`、単一文書、設定は CLI フラグ）。
ユーザ提示の 106 項目はほぼ全て**クローム層**（タブ帯・ブックマーク・オムニボックス・セッション永続化）の要求である。

本書はその 106 項目を 効用関数 U（w_footprint=w_correctness=0.20 優位、w_complexity 罰 0.20）で裁定した台帳である。
**これは実装計画ではない。** 採用キューであり、クローム系マイルストーンのコードは §1 の天井が採択されるまで一行も書かない。

## 1. 制約モデル（天井:**採択** 2026-07-29。ユーザ決定で v-chrome を v0.2 より前倒し）

数値なき軽量は最適化不能。クローム系初回リリース（TUI）の天井。超過したら機能追加ではなく削減か設計見直し:

| 指標 | 提案天井 | 根拠 |
|---|---|---|
| バイナリ合計（engine+chrome, stripped） | ≤ 200 KB | engine 実測 80 KB + chrome 推定 40–80 KB（入力/タブ帯/状態ストア/オムニボックスで C11 3–5K LOC → text 30–60 KB 程度。推定） |
| 空タブ UI 常駐 自己申告 RSS | ≤ 4 MB | 直前エンジン 18KB 文書で 1.88 MB 実測。クローム構造体は KB 級のはず（推定） |
| アンロード済み 50 タブのメタデータ | ≤ 2 MB | 1 タブあたり URL+title+scroll+group で ≤ 256 B 設計なら 12.8 KB。2 MB は 150 倍の余裕（推定） |
| 50 タブのセッション復元（描画は遅延） | ≤ 100 ms | メタだけ先読み、文書は選択時に読む（遅延ロード）。フラットテキスト 10^4 行 parse は ms 級（推定） |
| コールドスタート（TUI 常駐込み、空タブ描画まで） | ≤ 25 ms | CLI 1.1 ms 実測 + termios/初期化コスト（推定） |
| TUI アイドル CPU | 0 % | INV-5 執行可能形: イベント待ちは read ブロックのみ。タイマー駆動再描画の全面禁止 |
| セッション/ブックマーク永続形式 | フラットテキスト+binlog 的追記、tmp+rename+fsync | SQLite 不採用の根拠は §5 |

## 2. 採択：今日から拘束する不変条件（INV、コスト≈0）

これらは「将来のクロームに課す法」であり、今のコードも既に満たすか、満たすよう検査で担保できる。

| ID | 不変条件 | 検証/執行手段 |
|---|---|---|
| INV-1（#61,#63） | 新規タブは既定で空白。ホーム/ショートカット/ニュース類のコンテンツ機構を既定導入しない | 設定既定値の単体テスト |
| INV-2（#62, C4） | ユーザ明示操作なしに一切フェッチしない（プリフェッチ/推薦/テレメトリ全般） | コード監査: ネットワーク呼出し経路は入力イベント起点のみ（v0.3 以降 CI 静的チェック化を検討） |
| INV-3（#49,#56,#60） | オムニボックス候補は local-first。ローカル候補枯渇時のみ Web 検索を「提案」し、暗黙送信しない | オムニボックス結合テスト（M-chrome） |
| INV-4（#55,#65,#68,#72） | クローム構成要素は全て設定で非表示化可能。UI プリセットは「標準/最小」の 2 つのみ。行数予算 0/1/2 行 | 設定スキーマテスト |
| INV-5（#67） | アニメーション禁止（タイマー駆動の再描画なし。idle は read ブロック） | コードレビュー不変条件 |
| INV-6（#70） | キーボードフォーカスは常時可視（反転表示） | スナップショットテスト（M-chrome） |
| INV-7（#82,#83,#84,#85） | ログイン不要・同期は core に入れない・テレメトリのコード自体を書かない（切替でなく「存在しない」が正直な実装） | 依存・シンボル監査 |
| INV-8（#90） | どのタブへも任意の場所から ≤2 キーで到達可能 | キーマップ静的検査 |
| INV-9（#92） | 永続化する全データに発見可能なパスがある（`--show-paths` で一覧） | CLI ゴールデンテスト |
| INV-10（#93,#94） | ブックマークの開き方・グループ概念は全フロントエンドで単一セマンティクス | 仕様文書（本書 §4 台帳が基準） |
| INV-11（#2 由来） | タブ表現は常に 1 系統のみ（横帯と縦帯の二重表示を構造的に不可能にする） | レンダラ 1 実装 + 向きフラグ |

## 3. アーキテクチャ結合（裁定时に確定した構造的決定）

- **C1 タブ文書寿命 = arena スコープ**: 1 タブ 1 arena。アンロード = `if_arena_destroy`（正確・定数時間・断片化なし）。
  帰結: **「どのタブが重いか」は `reserved` バイトの表示で構造的にタダ**（Chrome/Firefox が苦しむ領域）。リンク一覧等のメタは doc arena に入れず chrome arena 側へ（アンロードで消えては困る）。
- **C2 永続化は単一ディレクトリ**（XDG 準拠、`--show-paths` で提示）。**tmp→rename→fsync** の原子書換（#19 「確実復元」の要件はこれに帰着する）。
- **C3 キーボード第一**: 全クローム操作がキーで完結。マウスは任意。
- **C4 バックグラウンド処理ゼロ**: タイマーは「ユーザ明示有効の自動アンロード」唯一の例外。
- **C5 Undo は 1 機構 3 用途**: 閉じたタブ/閉じたグループ/削除ブックマークを同一の capped ring で実装（#20,#33,#34。3 独立実装を struct 的に禁止）。

## 4. 裁定台帳（全 106 項目）

コード: **INV** = §2 の不変条件として採択 / **DEF** = クローム系マイルストーン採用キュー（core=必須級 / nice=余裕があれば / later=依存マイルストーン待ち） / **REJ** = 棄却（§5）/ **NA** = 端末主軸では非適用。

内訳（`awk` 実カウント、合計 106）: INV-only 19 / DEF core 40（+1 は INV 併記の #49）/ DEF nice 25 / DEF later 13 / REJ 3 / NA 4 / SAT 1。

### A. タブ/セッション
| # | 項目 | 裁定 | 一言根拠 |
|---|---|---|---|
| 1 | 縦タブ切替 | DEF core | 向きフラグのみ（INV-11） |
| 2 | 縦時に上バー消去 | DEF core | INV-11 により構造保証 |
| 3 | タブ幅固定 | DEF core | タブ帯描画オプション |
| 4 | タイトル省略ルール | DEF nice | 前方/中抜きの2択で十分 |
| 5 | タブ検索ポップアップ | DEF core | title+URL の in-memory 走査 |
| 6 | 同一URL→既存タブ | DEF core | URL 正規化後の完全一致のみ（曖昧一致は事故る） |
| 7 | 重複タブ一括整理 | DEF nice | #6 の表があればタダ |
| 8 | 選択タブまとめて閉じる | DEF core | 複数選択モデル要 |
| 9 | まとめて移動 | DEF nice | #8 のモデル流用 |
| 10 | まとめてピン | DEF nice | 同上 |
| 11 | グループ作成 | DEF core | group はタブのメタデータ 1 フィールド |
| 12 | グループ折りたたみ | DEF core | 同上 |
| 13 | グループ保存して閉じる | DEF core | セッションストア依存（C2） |
| 14 | 保存グループ再開 | DEF core | 同上 |
| 15 | グループ名検索 | DEF nice | #5 の走査流用 |
| 16 | グループを別窓へ | DEF later | v-chrome は単一ウィンドウ。窓モデル整備後 |
| 17 | ウィンドウ命名 | DEF later | 窓モデル後 |
| 18 | 多タブ時のみ終了警告 | DEF core | 閾値設定。無条件警告は INV 的に禁止 |
| 19 | 前回セッション確実復元 | DEF core | C2 の rename+fsync に帰着 |
| 20 | 閉じたグループ復元 | DEF nice | C5 の ring |
| 21 | 手動アンロード | DEF core | C1 により正確かつ定数時間 |
| 22 | 自動アンロード | DEF core | C4 の唯一例外。フォーム状態なしの v0.x では安全 |
| 23 | アンロード除外 | DEF nice | #22 の設定項目 |
| 24 | タイトル+URL 一括コピー | DEF core | OSC52/クリップボードは端末依存を明記 |
| 25 | タブを左右/末尾へ送る | DEF core | INV-8 に整合 |
| 26 | タブメモ（1行） | DEF nice | スキーマ v1 でフィールド予約済みにする |
| 27 | 新規タブ位置（右/末尾） | DEF core | 設定 1 項目 |
| 28 | 新規タブ位置固定 | DEF core | #27 に統合 |
| 29 | 閉じたタブ履歴検索 | DEF nice | C5 ring + #5 走査 |
| 30 | タブ数常時表示 | DEF core | 整数 1 個 |

### B. ブックマーク
| # | 項目 | 裁定 | 一言根拠 |
|---|---|---|---|
| 31 | ワンキー追加/削除 | DEF core | INV-8 整合 |
| 32 | 検索結果から直接編集 | DEF core | 検索 UI と編集の分離禁止 |
| 33 | ゴミ箱 | DEF core | C5 ring |
| 34 | 削除 Undo | DEF core | C5 ring |
| 35 | タグ | DEF nice | スキーマ v1 予約 |
| 36 | 短いメモ | DEF nice | 同上 |
| 37 | 別名（keyword） | DEF nice | オムニボックス dispatch。b/h/t との衝突規則要 |
| 38 | フォルダをタブグループとして開く | DEF core | 対称性（#39 と対） |
| 39 | タブグループをフォルダ保存 | DEF core | 同上。構造保存はインデント深さ字段り |
| 40 | 重複検出 | DEF core | URL 正規化 + 完全一致 |
| 41 | フォルダ単位ソート | DEF nice | qsort 1 回 |
| 42 | 最近追加順ビュー | DEF nice | added_at を schema v1 に必須化 |
| 43 | 最近使用順ビュー | DEF nice | last_used。書込は遅延・合体（キー打鍵ごとのストア書換は INV 的に禁止） |
| 44 | バー区切り/固定領域 | DEF nice | 描画オプション |
| 45 | 開き先制御（現/新/bg） | DEF core | INV-10 の単一セマンティクスで提供 |
| 46 | 「このプロファイルで開く」 | DEF later | プロファイル=名前付き dir。ネットワーク後 |
| 47 | favicon 遅延取得 | **REJ** | §5 |
| 48 | HTML/JSON インポート・エクスポート | DEF core | データ所有権。同期の代替（#84） |

### C. アドレスバー/検索
| # | 項目 | 裁定 | 一言根拠 |
|---|---|---|---|
| 49 | local-first オムニボックス | DEF core + INV-3 | 不変条件化済み |
| 50 | 候補優先順の設定 | DEF nice | 重みテーブル |
| 51 | b/h/t 前置検索 | DEF core | dispatch 3 行 |
| 52 | Enter→最上位ローカル候補 | DEF core | local-first の帰結 |
| 53 | =電卓/u単位変換 | **REJ** | §5 |
| 54 | 「既に開いている」強提示 | DEF core | #6 の URL 表 |
| 55 | 検索エンジン切替 UI を隠す | INV-4 | 既定で UI なし |
| 56 | ローカル候補を先に出し切る | INV-3 | #49 に統合 |
| 57 | 完全一致優先モード | DEF nice | フラグ |
| 58 | あいまい強度調整 | DEF nice | 単一アルゴリズム+閾値のみ（複数実装禁止） |
| 59 | ペースト URL 即判定 | DEF core | bracketed paste。端末依存明記 |
| 60 | 0 件時のみ Web 検索提案 | INV-3 | #49 に統合 |

### D. UI 最小化
| # | 項目 | 裁定 | 一言根拠 |
|---|---|---|---|
| 61 | 真っ白な新規タブ | INV-1 | 既定 |
| 62 | ニュース/フィード完全 OFF | INV-2 | 機構自体を持たない |
| 63 | ショートカット 0 個可 | INV-1 | 同上 |
| 64 | コンパクト密度 | DEF nice | 行高設定 |
| 65 | 全要素の個別表示/非表示 | INV-4 | スキーマで強制 |
| 66 | モノクロテーマ | DEF core | `--no-ansi` が前身 |
| 67 | アニメーション最小化 | INV-5 | ゼロで固定 |
| 68 | ツールバー高さ固定/最小 | INV-4 | 行数予算 0/1/2 |
| 69 | サイドバー幅記憶 | DEF nice | 設定値 |
| 70 | フォーカス常時可視 | INV-6 | a11y |
| 71 | ステータスバー任意表示 | DEF core | 行数予算内 |
| 72 | 2 モードのみ | INV-4 | 「標準/最小」固定 |
| 73 | タイトルバー統合 | NA | 端末に WM 無し。OSC で窓タイトルは設定可（#77 側） |
| 74 | 右クリック短縮 | DEF nice | メニューはキーボード駆動（C3） |

### E. プロファイル/プライバシー
| # | 項目 | 裁定 | 一言根拠 |
|---|---|---|---|
| 75 | ローカルプロファイル切替 | DEF core | プロファイル=dir、以上でも以下でもない |
| 76 | プロファイル色帯 | DEF nice | 1 行消費、既定 OFF 提案 |
| 77 | タイトルにプロファイル名 | DEF core | OSC タイトル |
| 78 | タスクバー分離 | NA | OS-GUI 概念 |
| 79 | サイト別権限一発リセット | DEF later | 権限ストアは v0.3 以降。仕様は今 |
| 80 | サイト別 Cookie 一発削除 | DEF later | Cookie ストア依存 |
| 81 | 一時セッション窓 | DEF core | 永続化しない dir。実装安く効用大 |
| 82 | ログイン不要 | INV-7 | 恒久 |
| 83 | 同期なしで成立 | INV-7 | 恒久 |
| 84 | 同期は後付け任意 | INV-7 | core に入れない。#48 が代替 |
| 85 | テレメトリ明示切替 | INV-7 | 更に強く: コード非存在 |
| 86 | 別プロファイルで開く | DEF later | #46 に統合 |

### F. モバイル/小画面
| # | 項目 | 裁定 | 一言根拠 |
|---|---|---|---|
| 87 | URL バー上下切替 | DEF nice | 行配置フラグ |
| 88 | スワイプでタブ移動 | NA | このコンテナで検証不能。将来のタッチ系フロントエンド |
| 89 | 片手モード | NA | 同上 |
| 90 | タブ一覧手数削減 | INV-8 | ≤2 キーで拘束 |
| 91 | グループ導線明快化 | DEF core | #11–15 の UX 仕様で拘束 |
| 92 | 保存先の見える化 | INV-9 | `--show-paths` |
| 93 | 開き方の前端統一 | INV-10 | 単一セマンティクス |
| 94 | グループ概念の統一 | INV-10 | 同上 |

### G. ダウンロード/閲覧補助
| # | 項目 | 裁定 | 一言根拠 |
|---|---|---|---|
| 95 | 保存前リネーム | DEF later | ダウンロード機構依存（v0.3+） |
| 96 | 保存先選択 | DEF later | 同上 |
| 97 | 簡素ダウンロード履歴 | DEF later | 追記専用テキストログ |
| 98 | 失敗再試行 | DEF later | ネットワーク依存 |
| 99 | 保存フォルダを開く | DEF later | xdg-open 存在時のみ。ない環境はパス表示+OSC8 |
| 100 | ページ内検索（部分一致/大小無視） | DEF core | グリッド走査 O(cells)、セル flag でハイライト |
| 101 | Reader=本文抽出のみ | DEF later | DOM テキスト走査の再利用（エンジンと相性良） |
| 102 | サイト別ズーム記憶 | **REJ（端末版）** | §5。意図は「サイト別 --width 記憶」で救済（DEF later） |
| 103 | シンプル印刷 | DEF nice | plain テキスト出力量出し |
| 104 | ページ情報簡易表示 | DEF core | `--stats` 配管は実在。Cookie/権限数は v0.3 後 |
| 105 | 可視スクショ（ターミナル版） | SAT（engine 級） | `--no-ansi > file` が既にそのもの。chrome キー割当ては DEF core |
| 106 | 簡素 HTML 保存 | DEF later | DOM シリアライザ（dump の拡張） |

## 5. 棄却リスト（根拠を数値で）

| # | 項目 | 棄却根拠 |
|---|---|---|
| 47 | favicon | 画像デコード（PNG/ICO）+ フェッチ配管という「未実装の 2 サブシステム」を要求し、文字グリッドへの情報量はゼロ。ランタイムコスト対効果が成立しない。GUI/ピクセル期に再審査 |
| 53 | 電卓/単位変換 | ブラウジング価値ゼロの式パーサ保守負債 + 攻撃面増。`bc` の管轄。w_complexity 罰を回収できない |
| 102 | サイト別ズーム | 整数セルグリッドにズーム概念は存在しない（グリフ 1=1 セル）。意図（見やすさ記憶）は「サイト別幅記憶」で満たす |

**SQLite 不採用（永続化全般の根拠）**: SQLite amalgamation（sqlite3.c）のコンパイル済み code サイズはおおよそ 1 MB 級（推定、-O2 x86-64）。**現行バイナリ全体（80,248 B 実測）の 12 倍超** をブックマーク管理のためだけに積むのは w_footprint=0.20 の下で即棄却。データ量（ブックマーク 10^4 件想定）に対しフラットテキストの parse は ms 級（推定）で十分。H5（実績ある部品の採用）は「コストが天井を破る」ため明示的根拠付きで棄却した、と記録する。

## 6. 順位づけと QuickJS との比較（U に基づく）

- 本 106 項目は**正確性（w_correctness=0.20）を一切改善しない** → v0.2 適合性マイルストーンより上位には置けない（U の直接帰結）。
- ただし **chrome/TUI は埋め込み JS（現 v0.4 QuickJS 案）より先に置くことを推薦**する。根拠:
  - フットプリント: chrome 推定 +40–80 KB vs QuickJS 推定 +600–900 KB（stripped）。**約 10 倍差**。
  - セキュリティ: chrome は新規攻撃面ほぼゼロ（端末エスケープ不変条件は既存） vs JS は JIT 相当の解釈系全体が面増加。
  - ユーザ提示の不満は全て chrome 層。robustness/maintainability 点も chrome が上。
- よってロードマップ案: v0.2（適合）→ v0.3（ネットワーク/ラスタ）→ **v-chrome（本書）** → JS（再査定付き）。v0.4 スロットの QuickJS は別途 U 再審査を要請する。

## 7. 本書の検証ステータス

- **slice-1（TUI クローム土台）は全項目実測済み**（値は BENCH.md 天井表の slice-1 列）。
- **slice-2（永続化 + 検索 + グループ最小形）完了・全項目実測済み**（BENCH.md slice-2 列）:
  - C2: `session.txt`（tmp→rename→fsync の原子上書き）を構造変化のたびに保存。
    scroll は構造変化では保存しない（書込増幅の禁止）が、quit 時に最終保存される。
  - セッション復元（メタのみ再現+active 即ロード+他は切替遅延ロード）**50 タブ 0.11 ms**
    で採択天井 ≤100 ms を 900× 上回る。復元不変条件（順序/scroll/group/active/next_id
    非衝突/遅延 doc=NULL）は単体テストで固定。
  - history.tsv（open 追記、512KB 超で半分縮退）/ bookmarks.tsv（URL 完全一致トグル、
    `b` / `B` overlay）、`@name` グループ（タブ帯 `[name]` 表示、セッション往復）。
  - **INV-8 完全形採用**: オムニボックス `?query` のライブ絞り込み＋選択相（1..9/Enter）。
    直近タブは 1 キー（1–9/タブ帯）、任意タブは「検索クエリ＋選択」の 2 系列で到達。
    「≤2 キー」を「2 入力系列まで」の運用意味論として採択（キー物理回数の厳密解では
    64 タブ全域を 2 打鍵で足りないことは算数的に確定しており、当初文言をこの解釈に
    訂正する）。
  - **INV-9 執行済み**: `--show-paths` が dir+3 ストアのパスとサイズを副作用ゼロで
    列挙（e2e 検査済み）。
- INV 残りの執行面: INV-3（オムニボックスの local-first 候補 = 履歴/ブックマーク連携、
  slice-3）/ INV-4（クローム要素の非表示設定、プリセット）/ INV-10（単一セマンティクスは
  構造的に守られている: chrome モデルが唯一の操作窓口）/ INV-11（タブ帯 1 系統、
  縦帯はピクセル期の向きフラグで対応）。
- エンジン側の現行実測値は BENCH.md を参照（バイナリは slice-2 で 133,752 B に増加、
  天井 200 KB の 1.5× 余裕）。

## 8. デザインコンセプト照合（`ifuto-browser.html`, cc1c652 でユーザ投入）

**読み取れた設計**: 横タブ帯 + ツールバー（ナビ/アドレス+⌘K ヒント/プロファイル）、アイコンレール、新タブ画面 = ダッシュボード（Favorites 3枠 / Continue reading 2件 / Focus session 25:00 タイマー / プライバシー宣言文 / Spaces 一覧 / Downloads（一時停止ボタン付）/ メモリメーター / トースト）。コピー: "with room to think"、"A browser that protects your attention"、"No tracking, no noise. browsing context on your device"。

**整合（設計が我々の不変条件・結合を追認）**:
- 「デバイスに残る・追跡しない」は製品公約となった → INV-7 は単なる方針でなく**ブランド約束**に格上げ。違反は虚偽表示になる。執行を強化（§2 参照、依存・シンボル監査を必須化）。
- Spaces = タブグループの一級市民化（#11–15 core の裏付け）。
- メモリメーター = C1（タブ=arena の正確 bite 計装）の需要証明。DEF core 内で優先度を最上位に。
- ⌘K ショートカットヒントの随所表示 = INV-8（≤2 キー到達）の UI 表現として採用。
- ニュース/広告タイルが設計に一切存在しない = INV-1/2 追認。

**衝突と調停（正直に）**:
1. **新タブダッシュボード vs INV-1（既定空白）**: 不変条件は維持。ダッシュボードはローカルストア（ブックマーク/履歴/リーディングリスト/Spaces/ダウンロード状態）のみで構築・ネットワークタイル禁止の「標準」プリセット収録機能とし、既定は空白（「最小」プリセット同等）。
2. **CSS transition/.2s アニメ vs INV-5**: TUI では無条件禁止を維持。ピクセル GUI 期に「reduced motion 既定 ON」で再査定する（再査定なし輸入は禁止）。
3. **@import Google Fonts**: 実行時フェッチは INV-2 違反。製品版はシステム/同梱フォントのみ。モック由来の依存を入れないルールを明記。
4. **窓ボタン/角丸 22px/シャドウ/グラデーション**: ピクセル時代（§5 のバックエンド境界より先）の話。TUI クロームへ落とすのは情報設計のみ（1セル罫線・影なし）。

**台帳の改訂（設計を根拠に）**:
- #47 favicon: **REJ を一部撤回**。設計の letter-tile（頭文字タイル）はフェッチ・画像デコード不要で TUI 互換 → letter-tile のみ DEF core で採用。ビットマップ favicon は引き続き REJ（ピクセル期再審査）。
- 新規採用キュー項目: **Continue reading（未読リスト）** = ブックマークの subtype（folder+進捗メタ）。DEF later。**ダウンロード一時停止/再開**（#98 と同マイルストーン）。**Focus session タイマー** = ブラウジング非機能だがエンジン影響ゼロ・~50 LOC 規模のクロームおまけ → DEF nice、ただし INV-4 で必ず非表示化可能とすること（複雑性罰より小さいと判断、再査定可）。
- toast 通知 = 一時通知要素。設定で無効化可能として DEF nice（#71 ステータス帯の兄弟分）。

**TUI 版への情報設計移植ルール**: rail=左 1–2 セル（隠せる）、tabstrip=0/1/2 行予算内、ダッシュボード=ローカル静的レンダリング（本エンジンで document として描画可能 = dogfood 済み: 255 nodes 0.47ms・crash なし）。
