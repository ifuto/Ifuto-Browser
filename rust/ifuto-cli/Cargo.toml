[package]
name = "ifuto-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Ifuto Browser の統合 CLI（C の src/main.c の完全置換。全観測モード + links/stats/show-paths/imgdecode + net/script 統合）"

[lints]
workspace = true

[[bin]]
name = "ifuto"
path = "src/main.rs"

[dependencies]
ifuto-core = { path = "../ifuto-core" }
ifuto-ffi = { path = "../ifuto-ffi" }
