[package]
name = "akl-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Aklus JS エンジンの C ABI ファサード（akl.h 互換）。ブラウザ統合用 FFI 層"

[lints]
workspace = true

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
akl-core = { path = "../akl-core" }
