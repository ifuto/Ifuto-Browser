[package]
name = "ifuto-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Ifuto Browser の最終統合層（<script> 実行配線 = akl-core と ifuto-core のクロスクレート配線）"

[lints]
workspace = true

[dependencies]
akl-core = { path = "../akl-core" }
ifuto-core = { path = "../ifuto-core" }
